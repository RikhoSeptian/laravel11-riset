import './bootstrap';
import { createApp } from "vue";
// import router from './router';
// import VueRouter from 'vue-router';
// import VueAxios from 'vue-axios';

import App from "./vue/App.vue";

createApp({})
    // .component('IncrementCounter', App)
    // .use(VueRouter)
    // .use(router)
    // .mount("#app")